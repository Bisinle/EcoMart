import React, { useState } from "react";
import { useAppContext } from "../MyContext";

export default function Profile() {
  // const { isLogedin, setIsLogedin, jwToken, setJwtToken } = useAppContext();
  // const [current_user_name, setCurrent_user_name] = useState(jwToken.user_name);
  // const [current_user_id, setCurrent_user_id] = useState(jwToken.user_id);
  // const [current_user_role, setCurrent_user_role] = useState(jwToken.user_role);
  // console.log(jwToken.user_name);
  // console.log(jwToken.user_id);
  // console.log(jwToken.user_role);

  return <div>Profile</div>;
}
